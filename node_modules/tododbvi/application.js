// status se TRUE = DA SVOLGERE se FALSE = SVOLTO
exports.todoList = todoList = [{
  idTodo:1,
  idCreatore:1,
  idAssegnatario : 3,
  azioneDaSvolgere: 'comprare il pane',
  completed:false
},
{
  idTodo:2,
  idCreatore:2,
  idAssegnatario : 1,
  azioneDaSvolgere: 'comprare il vino',
  completed:false
},
{
  idTodo:3,
  idCreatore:3,
  idAssegnatario : 2,
  azioneDaSvolgere: 'comprare la pasta',
  completed:false
},
{
  idTodo:4,
  idCreatore:4,
  idAssegnatario : 3,
  azioneDaSvolgere: 'comprare la carne',
  completed:false
},
{
  idTodo:5,
  idCreatore:5,
  idAssegnatario : 1,
  azioneDaSvolgere: 'comprare il mascarpone',
  completed:false
}];

exports.utenti = utenti = [{
  idCliente:1,
  name:'vincenzo'
},
{
  idCliente:2,
  name:'carlo'
},
{
  idCliente:3,
  name:'maria'
},
];


exports.add = function (idCreatore,idAssegnatario,azioneDaSvolgere) {
  cond=false; cond2=false; cond3=false;
  for (var i in utenti){
    if(idCreatore==utenti[i].idCliente){
      cond=true;
    }
    if(idAssegnatario==utenti[i].idCliente){
      cond2=true;
    }
  }
  if(idCreatore==idAssegnatario && idCreatore!=undefined && idAssegnatario!=undefined ){
    cond3=true;
  }

  if(cond3==true){
    message={message:"the id creator and id assignement are equal"};
    return message;
  }

  if(cond==false || cond2==false){
    message={message:"the body with creator or assignement user is not present"};
    return message;
  }
  if(azioneDaSvolgere==undefined){
    azioneDaSvolgere="undefined";
  }

  var newID = parseInt(todoList[todoList.length-1].idTodo)+1;

  todoList.push({
    idTodo:newID,
    idCreatore: idCreatore,
    idAssegnatario : idAssegnatario,
    azioneDaSvolgere : azioneDaSvolgere,
    completed:false
  });
  message={message:"the todo was been added"};

  return message;
};

exports.del = function(id){
  id = parseInt(id);
  check=false;
  for(var i=0;i<todoList.length;i++){
    if(id==todoList[i].idTodo){
      todoList.splice(i,1);
      check=true;
      messageDelete={message:"the todo was deleted successfully"};
      return messageDelete;
    }
  }
  if(check==false){
    messageDelete={message:"the todo is not present in the database"};
    return messageDelete;
  }
};

exports.setCompleted = function(id){
  id = parseInt(id);
  check=false;
  for(var i=0;i<todoList.length;i++){
    if(id==todoList[i].idTodo){
      todoList[i].completed=true;
      check=true;
      messageCompleted={message:"the todo was set completed"};
      return messageCompleted;
    }
  }
  if(check==false){
    messageCompleted={message:"the todo is not present in the database"};
    return messageCompleted;
  }

};

exports.setUncompleted = function(id){
  id = parseInt(id);
  check=false;
  for(var i=0;i<todoList.length;i++){
    if(id==todoList[i].idTodo){
      todoList[i].completed=false;
      check=true;
      messageUncompleted={message:"the todo was set Uncompleted"};
      return messageUncompleted;
    }
  }
  if(check==false){
    messageUncompleted={message:"the todo is not present in the database"};
    return messageUncompleted;
  }

};

exports.showCompletedTodos = function(){
  var todosComplete=[];
  check=false;
  for(var i=0;i<todoList.length;i++){
    if(todoList[i].completed==true){
      todosComplete.push(todoList[i]);
      check=true;
    }
  }

  if(check==false){
    message={message:"no todos completed avable"};
    return message;
  }else{
    return todosComplete;
  }

};

exports.showUncompletedTodos = function(){
  var todosUncomplete=[];
  check=false;
  for(var i=0;i<todoList.length;i++){
    if(todoList[i].completed==false){
      todosUncomplete.push(todoList[i]);
      check=true;
    }
  }

  if(check==false){
    message={message:"no todos UnCompleted avable"};
    return message;
  }else{
    return todosUncomplete;
  }

};

exports.showTodosCreator = function(user){
  var todos=[];
  check=false;
  for(var i=0;i<todoList.length;i++){
    if(todoList[i].idCreatore==user){
      todos.push(todoList[i]);
      check=true;
    }
  }

  if(check==false){
    message={message:"no todos whit this id"};
    return message;
  }else{
    return todos;
  }

};

exports.showTodosAssignee = function(user){
  var todos=[];
  check=false;
  for(var i=0;i<todoList.length;i++){
    if(todoList[i].idAssegnatario==user){
      todos.push(todoList[i]);
      check=true;
    }
  }

  if(check==false){
    message={message:"no todos whit this id"};
    return message;
  }else{
    return todos;
  }

};
